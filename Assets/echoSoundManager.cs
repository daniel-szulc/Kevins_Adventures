using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class echoSoundManager : MonoBehaviour

{

    
    /*void Awake()
    { 
        AudioSource[] soundobject = (AudioSource[])GameObject.FindObjectsOfType(typeof(AudioSource));

        foreach (var VARIABLE in soundobject)
        {
            VARIABLE.gameObject.AddComponent<AudioReverbFilter>();
            VARIABLE.gameObject.GetComponent<AudioReverbFilter>().reverbPreset = AudioReverbPreset.Cave;
        }
        
        
    }*/

}

